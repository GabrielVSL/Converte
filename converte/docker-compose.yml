services:
  converte:
    build: .
    container_name: converte
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./banco_local_pei.db:/app/banco_local_pei.db
    environment:
      - NODE_ENV=production
      - PORT=3000